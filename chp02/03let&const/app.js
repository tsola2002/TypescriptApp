var container = document.getElementById('container');
for (var x = 0; x <= 5; x++) {
    // lets you define variables
    // you can use to replace the var keyword
    // let counter = x;
    // const stands for constant which lets you define a variable that cannot be changed
    var counter = x;
    // const cannot be reassigned because its a constant
    //counter = 1;
}
console.log(counter);
